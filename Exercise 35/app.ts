// Shirt Making

const make_shirt = (size: string, color: string, message: string): void => {
    console.log(`Size: ${size}, Color: ${color}, Message: ${message}`);
}

make_shirt('M', 'Black', 'I like Black');